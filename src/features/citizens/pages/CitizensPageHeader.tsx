import { Stack, Typography } from '@mui/material'

export const CitizensPageHeader = () => (
    <Stack spacing={1}>
        <Typography variant="overline" color="primary.main">
            Реестр
        </Typography>
        <Typography variant="h4">Граждане</Typography>
    </Stack>
)
